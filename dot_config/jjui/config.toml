experimental_log_batching_enabled = false
[ui]
  # flexoki, ui color
  highlight_light = "#E6E4D9"
  highlight_dark = "#282726"
  theme = "flexoki-light"

# [ui.theme]
#   dark = "flexoki-dark"
#   light = "flexoki-light"

# [ui.colors]
#   "selected" = { "bg" = "black", underline = true, italic = true}

[custom_commands."new change after"]
key = ["N"]
args = ["new", "--no-edit", "--after", "$change_id"]
description = "Insert a new change after the selected one"
